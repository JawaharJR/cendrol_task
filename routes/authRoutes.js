const express = require("express");
const router = express.Router();
const { authenticateUser } = require("../middlewares/authMiddleware");

// Add authentication routes here, e.g., login and register

module.exports = router;
